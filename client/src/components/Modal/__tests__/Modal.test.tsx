import React from 'react';
import '@testing-library/jest-dom/extend-expect';

describe('Modal component', () => {
  it('should render the modal with correct contents when isModalOpen is true', () => {});
  it('should call handleModalSubmit() when modal is submitted successfully', () => {});
  it('should not call handleModalSubmit() when clicking the Close button', () => {});
  it('should call handleModalSubmit() and show response message when modal is submitted successfully', () => {});
  it('should not call handleModalSubmit() and show response message when modal is submitted with no note', () => {});
});
